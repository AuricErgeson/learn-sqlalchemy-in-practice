# Database Connection

- relational databases use the key or select
- all() method equal to return a list of objects
- first() return only the first result row or None
- one() return only the first and only one result

## Filters

- or_()
- where()
- between()

## Aggregation Functions

